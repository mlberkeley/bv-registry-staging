# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote aquila_umap 1.0` when ready.

[tool]
id = "aquila_umap"
version = "1.0"
description = "This is a program to generate Umap for Aquila diploid assembly."
homepage = "https://github.com/maiziex/Aquila_Umap"
license = "MIT"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/aquila_umap:1.0--py_0"
digest = "sha256:bfe47e5e6dd5aecd9d0f3c01461a9d5cd736a493e7557c77c70f885c9d23d679"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

[[tool.inputs]]
name = "reference"
type = "fasta"
cardinality = "one"
description = "Reference genome to map uniqueness for"

[[tool.outputs]]
name = "umap"
type = "dir"
cardinality = "one"
description = "Uniqueness (Umap) tracks for Aquila"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "aquila_umap"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "aquila_umap",
]