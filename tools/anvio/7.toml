# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote anvio 7` when ready.

[tool]
id = "anvio"
version = "7"
description = "A platform for integrated multi-omics"
homepage = "http://merenlab.org/software/anvio/"
license = "GPL-3.0-or-later"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/anvio:7--hdfd78af_1"
digest = "sha256:0bde9100ec18cef61457a716ba36a97d8b970b29409569362458f19f0632627e"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

# TODO: declare typed I/O before promoting to bv-registry.
# Available types: https://github.com/mlberkeley/bv/blob/main/bv-types/types.toml
#
# Example:
# [[tool.inputs]]
# name = "query"
# type = "fasta"
# cardinality = "one"
# description = "Input sequences"
#
# [[tool.outputs]]
# name = "output"
# type = "blast_tab"
# cardinality = "one"
# description = "Results"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "anvi-self-test"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "anvi-self-test",
]