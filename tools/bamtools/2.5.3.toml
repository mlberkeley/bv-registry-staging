# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote bamtools 2.5.3` when ready.

[tool]
id = "bamtools"
version = "2.5.3"
description = "C++ API & command-line toolkit for working with BAM data."
homepage = "https://github.com/pezmaster31/bamtools"
license = "MIT"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/bamtools:2.5.3--he132191_0"
digest = "sha256:b25c726fb17f173d0971615b5d907b11bfc12021b7a7744e36a630906998689b"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

# TODO: declare typed I/O before promoting to bv-registry.
# Available types: https://github.com/mlberkeley/bv/blob/main/bv-types/types.toml
#
# Example:
# [[tool.inputs]]
# name = "query"
# type = "fasta"
# cardinality = "one"
# description = "Input sequences"
#
# [[tool.outputs]]
# name = "output"
# type = "blast_tab"
# cardinality = "one"
# description = "Results"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "bamtools"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "bamtools",
]