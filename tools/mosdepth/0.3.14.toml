# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote mosdepth 0.3.14` when ready.

[tool]
id = "mosdepth"
version = "0.3.14"
description = "Fast BAM/CRAM depth calculation for WGS, exome, or targeted sequencing."
homepage = "https://github.com/brentp/mosdepth"
license = "MIT"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/mosdepth:0.3.14--h05c3d44_0"
digest = "sha256:50a99dc99f7ede1475976a3f562ffd682d839d69f68ce439f441dd553b3172bc"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

# TODO: declare typed I/O before promoting to bv-registry.
# Available types: https://github.com/mlberkeley/bv/blob/main/bv-types/types.toml
#
# Example:
# [[tool.inputs]]
# name = "query"
# type = "fasta"
# cardinality = "one"
# description = "Input sequences"
#
# [[tool.outputs]]
# name = "output"
# type = "blast_tab"
# cardinality = "one"
# description = "Results"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "mosdepth"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "mosdepth",
]