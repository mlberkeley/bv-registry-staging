# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote apollo 4.2.13` when ready.

[tool]
id = "apollo"
version = "4.2.13"
description = "WebApollo API library"
homepage = "https://github.com/galaxy-genome-annotation/python-apollo"
license = "MIT"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/apollo:4.2.13--pyh5e36f6f_0"
digest = "sha256:b5f6cbb720eeef1e27551a006167a5f306d487b4839326c82440035f5e6fc3ad"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

# TODO: declare typed I/O before promoting to bv-registry.
# Available types: https://github.com/mlberkeley/bv/blob/main/bv-types/types.toml
#
# Example:
# [[tool.inputs]]
# name = "query"
# type = "fasta"
# cardinality = "one"
# description = "Input sequences"
#
# [[tool.outputs]]
# name = "output"
# type = "blast_tab"
# cardinality = "one"
# description = "Results"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "arrow"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "arrow",
]