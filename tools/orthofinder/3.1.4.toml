# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote orthofinder 3.1.4` when ready.

[tool]
id = "orthofinder"
version = "3.1.4"
description = "Accurate inference of orthogroups, orthologues, gene trees and rooted species tree made easy!"
homepage = "https://github.com/OrthoFinder/OrthoFinder"
license = "GPL-3.0-only"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/orthofinder:3.1.4--hdfd78af_0"
digest = "sha256:94eae88393c1aefab39a33a04731cc616bee22514e24e0fa9834a66a832e3d6f"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

# TODO: declare typed I/O before promoting to bv-registry.
# Available types: https://github.com/mlberkeley/bv/blob/main/bv-types/types.toml
#
# Example:
# [[tool.inputs]]
# name = "query"
# type = "fasta"
# cardinality = "one"
# description = "Input sequences"
#
# [[tool.outputs]]
# name = "output"
# type = "blast_tab"
# cardinality = "one"
# description = "Results"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "orthofinder"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "orthofinder",
]