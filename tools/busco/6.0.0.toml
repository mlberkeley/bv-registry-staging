# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote busco 6.0.0` when ready.

[tool]
id = "busco"
version = "6.0.0"
description = "Assessment of assembly completeness using Universal Single Copy Orthologs."
homepage = "https://busco.ezlab.org"
license = "MIT"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/busco:6.0.0--pyhdfd78af_3"
digest = "sha256:9450959bc503533acf604c514712b9fd5944f9a277ef13c6026e9f4e188147cf"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

# TODO: declare typed I/O before promoting to bv-registry.
# Available types: https://github.com/mlberkeley/bv/blob/main/bv-types/types.toml
#
# Example:
# [[tool.inputs]]
# name = "query"
# type = "fasta"
# cardinality = "one"
# description = "Input sequences"
#
# [[tool.outputs]]
# name = "output"
# type = "blast_tab"
# cardinality = "one"
# description = "Results"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "busco"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "busco",
]