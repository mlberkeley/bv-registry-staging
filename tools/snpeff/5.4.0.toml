# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote snpeff 5.4.0` when ready.

[tool]
id = "snpeff"
version = "5.4.0"
description = "Genetic variant annotation and effect prediction toolbox"
homepage = "http://snpeff.sourceforge.net/"
license = "LGPLv3"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/snpeff:5.4.0c--hdfd78af_0"
digest = "sha256:832e86061ab3c1ef4339ec42f10fb98c4eaaecd498cd37d2bbe747e6bcc14de7"

[tool.metadata]
upstream_version = "5.4.0c"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

[[tool.inputs]]
name = "variants"
type = "vcf"
cardinality = "one"
description = "Variants to annotate"

[[tool.outputs]]
name = "annotated"
type = "vcf"
cardinality = "one"
description = "Variants annotated with predicted effects"

[[tool.outputs]]
name = "summary"
type = "file"
cardinality = "optional"
description = "HTML summary report (snpEff_summary.html)"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "snpEff"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "snpEff",
]