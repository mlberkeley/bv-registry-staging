# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote archer 0.1.1` when ready.

[tool]
id = "archer"
version = "0.1.1"
description = "Pre-process amplicon data before running CLIMB workflows."
homepage = "https://github.com/will-rowe/archer"
license = "MIT"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/archer:0.1.1--he881be0_0"
digest = "sha256:c0492839a938f5581c79409cf339c781dfb112f32b438eb89b17a5860d0c810a"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

# TODO: declare typed I/O before promoting to bv-registry.
# Available types: https://github.com/mlberkeley/bv/blob/main/bv-types/types.toml
#
# Example:
# [[tool.inputs]]
# name = "query"
# type = "fasta"
# cardinality = "one"
# description = "Input sequences"
#
# [[tool.outputs]]
# name = "output"
# type = "blast_tab"
# cardinality = "one"
# description = "Results"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "archer"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "archer",
]