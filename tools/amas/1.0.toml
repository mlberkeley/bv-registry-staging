# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote amas 1.0` when ready.

[tool]
id = "amas"
version = "1.0"
description = "Calculate various summary statistics on a multiple sequence alignment"
homepage = "https://github.com/marekborowiec/AMAS"
license = "GPL-3.0-or-later"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/amas:1.0--pyh864c0ab_0"
digest = "sha256:5d12dc3061fb56a198d355244075a6dd8bd98771432954e5a1018a8cd81124fc"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

# TODO: declare typed I/O before promoting to bv-registry.
# Available types: https://github.com/mlberkeley/bv/blob/main/bv-types/types.toml
#
# Example:
# [[tool.inputs]]
# name = "query"
# type = "fasta"
# cardinality = "one"
# description = "Input sequences"
#
# [[tool.outputs]]
# name = "output"
# type = "blast_tab"
# cardinality = "one"
# description = "Results"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "AMAS.py"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "AMAS.py",
]