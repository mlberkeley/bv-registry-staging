# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote alignment 1.0.10` when ready.

[tool]
id = "alignment"
version = "1.0.10"
description = "Native Python library for generic sequence alignment."
homepage = "https://github.com/eseraygun/python-alignment"
license = "BSD 3-Clause"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/alignment:1.0.10--pyh5e36f6f_0"
digest = "sha256:03eeb9788d1295c9211ad078219d8c58d837faf70b4440fb21e0db8acad9bb59"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

# TODO: declare typed I/O before promoting to bv-registry.
# Available types: https://github.com/mlberkeley/bv/blob/main/bv-types/types.toml
#
# Example:
# [[tool.inputs]]
# name = "query"
# type = "fasta"
# cardinality = "one"
# description = "Input sequences"
#
# [[tool.outputs]]
# name = "output"
# type = "blast_tab"
# cardinality = "one"
# description = "Results"

[tool.entrypoint]
# TODO: set the correct entrypoint command
command = "alignment"

# Derived from entrypoint only. Add other binaries this tool exposes.
[tool.binaries]
exposed = [
  "alignment",
]