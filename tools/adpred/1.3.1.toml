# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote adpred 1.3.1` when ready.

[tool]
id = "adpred"
version = "1.3.1"
description = "python adpred module for prediction of Transcription activation domains from protein sequences"
homepage = "https://github.com/FredHutch/adpred"
license = "MIT Licence"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/adpred:1.3.1--pyhdfd78af_0"
digest = "sha256:e41d82accfec19d6a900f146c0e7fdd94b97a4ac3c9af6334dc0bd88c2a7de4d"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

# TODO: declare typed I/O before promoting to bv-registry.
# Available types: https://github.com/mlberkeley/bv/blob/main/bv-types/types.toml
#
# Example:
# [[tool.inputs]]
# name = "query"
# type = "fasta"
# cardinality = "one"
# description = "Input sequences"
#
# [[tool.outputs]]
# name = "output"
# type = "blast_tab"
# cardinality = "one"
# description = "Results"

[tool.entrypoint]
# TODO: set the correct entrypoint command
command = "adpred"

# Derived from entrypoint only. Add other binaries this tool exposes.
[tool.binaries]
exposed = [
  "adpred",
]