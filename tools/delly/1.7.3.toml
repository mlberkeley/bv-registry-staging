# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote delly 1.7.3` when ready.

[tool]
id = "delly"
version = "1.7.3"
description = "Structural variant discovery by integrated paired-end and split-read analysis."
homepage = "https://github.com/dellytools/delly"
license = "BSD-3-Clause"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/delly:1.7.3--hd6466ae_0"
digest = "sha256:97e6daaa0b8c4f0e4c77066c4152cef169ffe517ee816bc77bf8637d1f25a592"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

[[tool.inputs]]
name = "alignments"
type = "bam"
cardinality = "many"
description = "Sorted, indexed BAM alignments"

[[tool.inputs]]
name = "reference"
type = "fasta"
cardinality = "one"
description = "Reference genome"

[[tool.outputs]]
name = "variants"
type = "bcf"
cardinality = "one"
description = "Structural-variant calls"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "delly"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "delly",
]