# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote afwdist 1.0.0` when ready.

[tool]
id = "afwdist"
version = "1.0.0"
description = "A lightweight tool to calculate a pairwise distance metric based on fixed and non-fixed allele frequencies."
homepage = "https://github.com/PathoGenOmics-Lab/afwdist"
license = "GPL-3.0-only"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/afwdist:1.0.0--h4349ce8_0"
digest = "sha256:fe1bc4878ba580700413c5cb1ce22a9c6aac3d1b8d2a9269bef03806369f7330"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

# TODO: declare typed I/O before promoting to bv-registry.
# Available types: https://github.com/mlberkeley/bv/blob/main/bv-types/types.toml
#
# Example:
# [[tool.inputs]]
# name = "query"
# type = "fasta"
# cardinality = "one"
# description = "Input sequences"
#
# [[tool.outputs]]
# name = "output"
# type = "blast_tab"
# cardinality = "one"
# description = "Results"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "afwdist"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "afwdist",
]