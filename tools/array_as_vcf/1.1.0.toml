# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote array_as_vcf 1.1.0` when ready.

[tool]
id = "array_as_vcf"
version = "1.1.0"
description = "Convert SNP array to VCF"
homepage = "https://github.com/LUMC/array-as-vcf"
license = "MIT"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/array-as-vcf:1.1.0--pyhdfd78af_0"
digest = "sha256:8fd44cf7649dfd57a7ec6c5398e2a43bafef26e6d1526c4de190220aa0cd0d24"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

# TODO: declare typed I/O before promoting to bv-registry.
# Available types: https://github.com/mlberkeley/bv/blob/main/bv-types/types.toml
#
# Example:
# [[tool.inputs]]
# name = "query"
# type = "fasta"
# cardinality = "one"
# description = "Input sequences"
#
# [[tool.outputs]]
# name = "output"
# type = "blast_tab"
# cardinality = "one"
# description = "Results"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "array-as-vcf"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "array-as-vcf",
  "aav",
]