# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote alphafill 2.3.0` when ready.

[tool]
id = "alphafill"
version = "2.3.0"
description = "Transplant missing compounds to the AlphaFold models"
homepage = "https://alphafill.eu"
license = "BSD-2-Clause"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/alphafill:2.3.0--h7f3286b_0"
digest = "sha256:8ab4086fa5f51e66e26f3c7bc1d8c6cc45711a9f8f0669249be5088b4b7386a8"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

# TODO: declare typed I/O before promoting to bv-registry.
# Available types: https://github.com/mlberkeley/bv/blob/main/bv-types/types.toml
#
# Example:
# [[tool.inputs]]
# name = "query"
# type = "fasta"
# cardinality = "one"
# description = "Input sequences"
#
# [[tool.outputs]]
# name = "output"
# type = "blast_tab"
# cardinality = "one"
# description = "Results"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "alphafill"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "alphafill",
]