# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote amused 1.0` when ready.

[tool]
id = "amused"
version = "1.0"
description = "Auditing Motifs Using Statistical Enrichment & Depletion"
homepage = "https://github.com/Carldeboer/AMUSED"
license = "GPL-2.0"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/amused:1.0--0"
digest = "sha256:c31cba4ba2e1d9daa2d01ca5b38e19deaa37cc5719bea9a16e1d96901145756c"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

[[tool.inputs]]
name = "sequences"
type = "fasta"
cardinality = "many"
description = "Sequences to scan for motif enrichment"

[[tool.outputs]]
name = "enrichment"
type = "tabular"
cardinality = "one"
description = "Motif enrichment and depletion statistics"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "AMUSED"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "AMUSED",
  "AMUSED-KS",
  "alignKMers",
]