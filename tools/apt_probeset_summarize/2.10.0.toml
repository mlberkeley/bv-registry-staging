# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote apt_probeset_summarize 2.10.0` when ready.

[tool]
id = "apt_probeset_summarize"
version = "2.10.0"
description = "From Affymetrix Power Tools package. apt-probeset-summarize is program for analyzing expression arrays including 3' IVT and exon arrays. Supports background correction (MAS5,RMA), normalization (linear scaling, quantile, sketch), and summarization (PLIER, RMA, MAS5) methods."
homepage = "https://downloads.thermofisher.com"
license = "GNU GENERAL PUBLIC LICENSE Version 2"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/apt-probeset-summarize:2.10.0--h9948957_6"
digest = "sha256:edf4480151452b3a85001f868b92477a9d88d36b43de6b28517916f9973de4fd"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

# TODO: declare typed I/O before promoting to bv-registry.
# Available types: https://github.com/mlberkeley/bv/blob/main/bv-types/types.toml
#
# Example:
# [[tool.inputs]]
# name = "query"
# type = "fasta"
# cardinality = "one"
# description = "Input sequences"
#
# [[tool.outputs]]
# name = "output"
# type = "blast_tab"
# cardinality = "one"
# description = "Results"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "apt-probeset-summarize"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "apt-probeset-summarize",
]