# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote acdc 1.02` when ready.

[tool]
id = "acdc"
version = "1.02"
description = "(a)utomated (c)ontamination (d)etection and (c)onfidence estimation for single-cell genome data"
homepage = "https://github.com/mlux86/acdc"
license = "MIT"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/acdc:1.02--h4ac6f70_0"
digest = "sha256:51adbf9cddd14884b1a59eba93db3d4106ba0bf331c39936583e1dd0ced95de3"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

[[tool.inputs]]
name = "contigs"
type = "fasta"
cardinality = "one"
description = "Single-cell or metagenomic contigs"

[[tool.outputs]]
name = "report"
type = "dir"
cardinality = "one"
description = "Contamination-detection report and cluster plots"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "acdc"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "acdc",
]