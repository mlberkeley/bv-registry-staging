# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote adapterremovalfixprefix 0.0.5` when ready.

[tool]
id = "adapterremovalfixprefix"
version = "0.0.5"
description = "Fixes adapter removal prefixes to make sure no clashing read names are in the output."
homepage = "https://github.com/apeltzer/AdapterRemovalFixPrefix"
license = "GPLv3"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/adapterremovalfixprefix:0.0.5--0"
digest = "sha256:89772bd4663220532fe096cba9bf215bb0a5ccfa9bd7e850e5f7574001faff14"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

[[tool.inputs]]
name = "reads"
type = "fastq"
cardinality = "one"
description = "Reads whose adapter-removal prefixes need fixing"

[[tool.outputs]]
name = "fixed"
type = "fastq"
cardinality = "one"
description = "Reads with de-duplicated read-name prefixes"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "AdapterRemovalFixPrefix"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "AdapterRemovalFixPrefix",
]