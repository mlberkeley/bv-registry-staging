# Auto-generated from Bioconda recipe by bv-ingest.
# Fill in [[tool.inputs]] and [[tool.outputs]] before promoting to bv-registry.
# Run `bv-ingest promote adapt 1.6.0` when ready.

[tool]
id = "adapt"
version = "1.6.0"
description = "A package to efficiently design activity-informed nucleic acid diagnostics for viruses."
homepage = "https://github.com/broadinstitute/adapt"
license = "MIT"

[tool.image]
backend = "docker"
reference = "quay.io/biocontainers/adapt:1.6.0--pyhdfd78af_0"
digest = "sha256:3a5f50552e7fdf37ade4b4ff36c0440291ed94a61a4750970258362b19eda343"

[tool.hardware]
# TODO: update if this tool has known hardware requirements
cpu_cores = 1
ram_gb = 2.0
disk_gb = 1.0

[[tool.inputs]]
name = "sequences"
type = "fasta"
cardinality = "one"
description = "Target viral genome sequences"

[[tool.outputs]]
name = "designs"
type = "tabular"
cardinality = "one"
description = "Ranked guide/primer designs"

[tool.entrypoint]
# Auto-detected from recipe test commands. Verify this is correct.
command = "design.py"

# Auto-detected from recipe test commands. Verify and add any missing binaries.
[tool.binaries]
exposed = [
  "design.py",
  "design_naively.py",
]